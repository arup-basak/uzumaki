---
title: Uzumaki
description: Build native desktop apps with React, TypeScript, and the Uzumaki runtime.
template: splash
hero:
  tagline: Native desktop apps in TypeScript. No browser. No webview. Just a small runtime and real windows.
  actions:
    - text: Start the Tutorial
      link: /guides/quick-start/
      icon: right-arrow
    - text: Install Uzumaki
      link: /guides/installation/
      variant: minimal
    - text: GitHub
      link: https://github.com/golok727/uzumaki
      icon: external
      variant: minimal
---

import { Card, CardGrid, LinkCard, Aside, Tabs, TabItem } from '@astrojs/starlight/components';

<Aside type="caution" title="Alpha runtime">
  These docs target `0.0.1-alpha.2`, which is not released yet. Uzumaki is early, so the docs teach the current runtime honestly while APIs are still settling.
</Aside>

## Install

<Tabs syncKey="platform">
  <TabItem label="Windows">

```sh
powershell irm https://uzumaki.run/install.ps1 | iex
```

  </TabItem>
  <TabItem label="macOS & Linux">

```sh
curl -fsSL https://uzumaki.run/install.sh | sh
```

  </TabItem>
</Tabs>

Then create an app:

```sh
uzumaki init my-app
cd my-app
pnpm install
pnpm dev
```

## What is Uzumaki?

<p class="uz-lead">Uzumaki is a desktop UI runtime for people who like to write TypeScript but do not want to ship a browser.</p>

You write TypeScript and JSX, and Uzumaki opens real native windows. There is no embedded browser and no webview — what you ship is a native app, not a web app in disguise.

## What Makes It Different?

- **Renderer-agnostic.** React is the first renderer; Solid, Vue, and Svelte are on the roadmap.
- **Single process.** Writing an app feels like writing a native app, not a main/renderer split.
- **No IPC bridge.** Your UI code and your runtime code share the same JavaScript context.
- **Node and Deno APIs work directly.** Built on Deno, with `node:*` compatibility — no shims, no bridge calls.

## Choose Your Path

<CardGrid>
  <LinkCard
    title="Learn the runtime"
    href="/guides/quick-start/"
    description="Build a small app from an empty scaffold and learn the pieces as they appear."
  />
  <LinkCard
    title="Understand the model"
    href="/concepts/how-it-works/"
    description="See how windows, elements, events, resources, and React fit together."
  />
  <LinkCard
    title="Look up an API"
    href="/reference/runtime-api/"
    description="Find the supported elements, props, events, window options, and runtime APIs."
  />
</CardGrid>

Start with [Quick Start](/guides/quick-start/) if you want the guided path.
